# Python Packaging Index.
# we are having standard libraries.
# We can share the python packages share on the internet.
# We are having really amazing libraries like numpy, tensorflow, and many more.
# using pip install command we use those libraries to install.
# we are having pypi.org

# pip install
