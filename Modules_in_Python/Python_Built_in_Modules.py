# Python Built in Modules.
# We will be loaded with built-in functions when you are installed Python on a system.

# import random

# print(random.shuffle([1, 2, 3, 4, 5, 6]))

# If we want to import a module as alias - We can use import <module_name> as <alias_name>

# In module import sys, sys.args will be having file name as the first variable.
