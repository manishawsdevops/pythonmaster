#Expressions vs Statements
iq = 100
result = iq / 10

#Here the "iq / 10" is considered as expression and the full line of code "result = iq / 10" is considered
# as the statement. Expressions are piece of code that produces a value.